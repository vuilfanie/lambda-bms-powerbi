service: streamingbms

# configValidationMode: off


provider:
  name: aws
  runtime: nodejs16.x
  stage: prod
  region: us-east-1
  memorySize: 128

functions:
  getBMStickets:
    handler: handler.getBMStickets

    events:
      - schedule: 
          rate: rate(15 minutes)
          enabled: true

    # environment: ${file(env.yml)}
